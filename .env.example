# LLM設定（オプショナル）
# DRY_RUN: モック応答を使用（デフォルト）
# OPENAI: OpenAI APIを使用
LLM_MODE=DRY_RUN

# OpenAI APIキー（LLM_MODE=OPENAIの場合のみ必要）
OPENAI_API_KEY=

# LLMモデル名（LLM_MODE=OPENAIの場合）
LLM_MODEL=gpt-4

# 作業ディレクトリ（ツール実行の制限範囲）
WORK_DIR=./workspace

# データベースファイル
DB_PATH=./ajson.db

# === LLM Gateway Configuration (Phase 7) ===
# Provider selection: DRY_RUN (default), OPENAI, GEMINI
LLM_PROVIDER=DRY_RUN

# Enable paid API calls (must be "1" to use external APIs)
LLM_ENABLE_PAID=0

# API Keys (required only for paid providers)
OPENAI_API_KEY=sk-...your-key...
GEMINI_API_KEY=...your-key...

# Model overrides (optional, defaults are low-cost)
OPENAI_MODEL=gpt-4o-mini
OPENAI_HIGH_COST_MODEL=gpt-4o
GEMINI_MODEL=gemini-1.5-flash
GEMINI_HIGH_COST_MODEL=gemini-1.5-pro

# Daily budget limit in USD (circuit breaker threshold)
LLM_DAILY_BUDGET_USD=3.00
